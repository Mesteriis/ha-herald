# Development Plan

## Status

Current milestone: `0.3.0` HACS-ready distribution with actionable feedback loops and dashboard presets.

## Completed

- Core async integration with config flow, diagnostics, services, sensors, queue, AI client, presence routing.
- Flow abstraction and YAML-first configuration model.
- Actionable notifications for mobile and Telegram.
- Local bundled frontend card served by the integration.
- Repository scaffolding for CI/CD and contribution workflow.
- Expanded runtime coverage for routing, config parsing, translation helpers, and actionable payloads.
- Push feedback loop with mobile clear handling and localized Telegram callback confirmations.
- Dashboard generation presets for `overview`, `rooms`, and `roles`.
- Release automation with `release-please` semantic version bumping.
